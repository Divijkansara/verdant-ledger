/* config.js — the only file to edit when deploying.
   Put your deployed backend's address here (no trailing slash), e.g.
   "https://verdant-ledger-iota.vercel.app". Leave it empty to use the
   local API at http://127.0.0.1:8000. */
window.VERDANT_API = "";
